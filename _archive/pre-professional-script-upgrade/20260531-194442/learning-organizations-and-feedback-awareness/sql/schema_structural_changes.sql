CREATE TABLE IF NOT EXISTS structural_changes (
  period INTEGER,
  unit_id TEXT,
  feedback_routine_change INTEGER,
  metric_change INTEGER,
  authority_change INTEGER,
  memory_system_change INTEGER,
  workload_redesign INTEGER,
  trust_repair_action INTEGER
);
