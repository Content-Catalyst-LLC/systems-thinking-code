CREATE TABLE IF NOT EXISTS defensive_routines (
  period INTEGER,
  unit_id TEXT,
  blame_language INTEGER,
  polished_reporting INTEGER,
  avoided_topics INTEGER,
  ritual_meetings INTEGER,
  action_items_without_change INTEGER
);
