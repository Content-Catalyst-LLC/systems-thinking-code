# Python Workflow

The default Python workflow is dependency-light and runs with the standard library. It produces synthetic scenario outputs for disease dynamics, care stress, public trust, health risk, and prevention value.

Run:

```bash
python3 python/run_all_public_health_workflows.py
```
