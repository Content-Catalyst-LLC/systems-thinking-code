# Python Workflow

Run:

```bash
python3 python/run_all_platform_workflows.py
```

The Python workflows create dependency-light synthetic platform feedback outputs, validate bounded indicators, and export diagnostic tables for engagement amplification, moderation capacity, platform dependency, and public-value governance.
