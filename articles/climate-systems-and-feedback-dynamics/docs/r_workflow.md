# R Workflow: Climate Feedback Indicators, Scenario Diagnostics, and Trajectory Visualization

The default R workflow uses base R only. It reads Python-generated CSV outputs and creates trajectory figures and diagnostic tables.

Scripts:

- `climate_feedback_diagnostics.R`
- `run_all_climate_workflows.R`

No tidyverse dependency is required for the default smoke test.
