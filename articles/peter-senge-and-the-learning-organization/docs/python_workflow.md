# Python Workflow

Default Python workflows use only the standard library.

Run:

```bash
python3 python/run_all_senge_learning_organization_workflows.py
```

Expected outputs:

- `outputs/tables/senge_learning_organization_timeseries.csv`
- `outputs/tables/senge_learning_organization_summary.csv`
- additional diagnostic CSVs when optional scripts are present
