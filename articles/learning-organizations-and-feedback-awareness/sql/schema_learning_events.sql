CREATE TABLE IF NOT EXISTS learning_events (
  period INTEGER,
  unit_id TEXT,
  feedback_items INTEGER,
  issues_discussed INTEGER,
  assumptions_challenged INTEGER,
  decisions_changed INTEGER,
  structural_changes INTEGER
);
