# R Workflow

The default R workflow uses base R only. It reads the Python outputs, creates scenario summaries, exports stress and trust diagnostics, and produces behavior-over-time plots.

Run:

```bash
Rscript r/run_all_public_health_workflows.R
```
