# Outputs

Generated figures and tables should be written here by the scripts in `python/`, `r/`, and other language folders.

Suggested workflow:

1. Run Python scripts to generate CSV tables.
2. Run R scripts to summarize or visualize the generated tables.
3. Save figures in `outputs/figures/` and tables in `outputs/tables/`.
