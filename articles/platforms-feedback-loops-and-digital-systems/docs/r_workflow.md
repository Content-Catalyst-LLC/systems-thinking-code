# R Workflow

Run after the Python workflow:

```bash
Rscript r/run_all_platform_r_workflows.R
```

The base R workflow reads Python-generated outputs and creates summary tables and trajectory figures using only base R.
