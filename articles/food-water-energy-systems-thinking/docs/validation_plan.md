# Validation Plan

The default workflow performs basic structural validation:

- Required CSV outputs exist.
- Required columns are present.
- Core index values are non-negative.
- Scenario summaries are generated.
- Base R diagnostics can read Python outputs.

For real analysis, add validation against observed groundwater levels, crop yields, energy demand, water-use records, climate indicators, household vulnerability data, and local governance assumptions.
