# Outputs

Generated figures and tables should be written to:

- `outputs/figures/`
- `outputs/tables/`

The scripts in `python/` and `r/` create simple CSV summaries and visualizations from the synthetic datasets.
