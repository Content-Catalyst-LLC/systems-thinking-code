# Python Workflow

Run from the article folder:

```bash
python3 python/run_all_urban_workflows.py
```

The default workflow uses only the Python standard library and exports CSV diagnostics to `outputs/tables`.
