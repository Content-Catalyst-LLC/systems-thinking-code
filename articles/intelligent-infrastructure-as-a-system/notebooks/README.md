# Notebooks

Notebook placeholders are included for teams that want interactive walkthroughs. The default repository workflow is script-first so it can be smoke-tested and reproduced easily from the command line.
