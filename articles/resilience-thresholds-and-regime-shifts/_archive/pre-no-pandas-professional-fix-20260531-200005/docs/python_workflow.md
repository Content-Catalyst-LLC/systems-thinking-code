# Python Workflow

The Python workflow generates synthetic resilience scenarios, threshold diagnostics, early warning indicators, hysteresis and recovery analysis, adaptive-capacity scenarios, sensitivity outputs, and distributional vulnerability tables.

Run from the article folder:

```bash
python3 python/run_all_resilience_workflows.py
```
