# R Workflow

The R workflow reads Python-generated outputs and produces base-R diagnostic plots and summary tables for resilience trajectories, threshold margins, early warning indicators, and vulnerability diagnostics.

Run from the article folder:

```bash
Rscript r/run_all_resilience_workflows.R
```
