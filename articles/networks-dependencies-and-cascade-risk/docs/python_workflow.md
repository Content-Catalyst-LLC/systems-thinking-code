# Python Workflow

Run:

```bash
python3 python/run_all_network_workflows.py
```

The workflow creates node criticality tables, dependency matrices, cascade simulations, bridge-node diagnostics, category summaries, and validation reports.
